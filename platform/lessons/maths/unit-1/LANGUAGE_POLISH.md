# Unit 1 Language Polish

Pass to remove invented terminology, clever-sounding labels, and over-formal phrasing across all of Unit 1 (Number). Goal: a 15-year-old in a Lahore classroom should feel comfortable repeating any sentence to a friend.

## Summary

- Total files reviewed: **88** (p1.md through p7.md plus review.md across 18 sub-topics)
- Files edited: **34**
- Files left unchanged: **54** (already in clean teacher voice)

## Files edited

### 1.1 Types of number
- `01-01-types-of-number/p6.md`: replaced "biggest shared chunk" with plainer description; "the whole trick" → "all there is to it".
- `01-01-types-of-number/p7.md`: removed "the trick is the same every time", "trickier case", "the trick is to multiply by 10"; rephrased "shift one full period of the repeat" → "shifts the decimal across one full repeat".
- `01-01-types-of-number/review.md`: "shift the recurring tail" → "the repeating digits line up".

### 1.2 Sets
- `01-02-sets/p3.md`: "lens-shaped overlap" → "the bit in the middle where the two circles cross over" (this was the example the user explicitly flagged).
- `01-02-sets/p4.md`: "fix is a single rule" → "fix is simple".

### 1.3 Powers and roots
- `01-03-powers-and-roots/p1.md`: "Squaring kills the sign" → "Squaring always gives a positive answer".

### 1.4 Fractions, decimals, percentages
- `01-04-fractions-decimals-percentages/p1.md`: "The recipe is multiply, then add, keep the bottom" → "Three steps: multiply, then add, keep the bottom".
- `01-04-fractions-decimals-percentages/p4.md`: removed "shift one full period", "cancel the recurring tail" in three places; replaced with "the repeating digits line up" / "the repeating digits cancel".

### 1.5 Ordering
- `01-05-ordering/p2.md`: "The trick is always the same" → "The method is always the same".

### 1.6 Four operations
- `01-06-four-operations/p1.md`: "the two minus signs eat each other" → "the two minus signs cancel out"; "The shortcut" → "The rule is short".
- `01-06-four-operations/p2.md`: "The recipe is keep, flip, multiply" → "Three steps: keep, flip, multiply".
- `01-06-four-operations/p3.md`: "The decimal point is the anchor. Pad with trailing zeros" → "Stack the numbers so the decimal points sit directly above each other... add zeros at the end so both numbers have the same length"; "the trick is to shift the decimal point" → "To make it easier, shift..."; "padding zeros" → "extra zeros"; "Pad to" → "Write as"; "pad with zeros" replaced everywhere.

### 1.7 Indices I
- `01-07-indices-i/p2.md`: "This is a fast trick on Paper 1" → "A quick way to handle these on Paper 1".
- `01-07-indices-i/p4.md`: "The Paper 1 routine" → "For any messy indices expression on Paper 1, work through these steps".

### 1.8 Standard form
- `01-08-standard-form/p2.md`: "pad with 3 zeros" / "pad with a leading zero" / "pad with zeros to fill any gap" → all rewritten as "add zeros to fill the gap" or "add a leading zero in front".

### 1.9 Estimation (the file the user explicitly flagged)
- `01-09-estimation/p1.md`: removed every use of "decider"; rule is now "Look at the digit just after the cut. If it is 5 or more, round up. If it is 4 or less, round down". Updated explain text in checks; common mistakes; quick summary; carry-up explanation now says "add 1 to the next column on the left, just like normal column addition".
- `01-09-estimation/p2.md`: "decider" stripped from all check explanations, the rounding rule, the diagram, the worked example, common mistakes, and quick summary; "pad with zeros" → "fill with zeros" / "add zeros to keep the size".
- `01-09-estimation/p3.md`: "keep the first non-zero digit and pad with zeros" → "keep the first non-zero digit, fill the rest with zeros".
- `01-09-estimation/review.md`: "decider" stripped from check explanations; "pad with zeros" → "add zeros".

### 1.10 Limits of accuracy
- `01-10-limits-of-accuracy/p2.md`: "The shortcut" → "A simple way to remember it".

### 1.11 Ratio and proportion
- `01-11-ratio-and-proportion/p1.md`: "The trick" → "The rule".
- `01-11-ratio-and-proportion/p2.md`: "The trick" → "The idea".
- `01-11-ratio-and-proportion/p3.md`: "the unitary method" softened to "sometimes called the unitary method"; M1 mark annotation rewritten without the term; common-mistake rewritten without "in the unitary method".
- `01-11-ratio-and-proportion/review.md`: "Proportional reasoning with the unitary method" → "Proportional reasoning by finding the value of one unit, then multiplying".

### 1.12 Rates
- `01-12-rates/p1.md`: "The pattern" → "The rule".
- `01-12-rates/p2.md`: "The trick" → "How to use it"; "For shortcut, you can also do" → "A quicker version: just divide by 3.6".

### 1.13 Percentages
- `01-13-percentages/p1.md`: "The shortcut for nice percentages" → "For round percentages, it's often quickest to use the fraction form".
- `01-13-percentages/p2.md`: removed "the multiplier" branding — "The fastest method is the multiplier" → "The fastest way is to use a single multiplier"; section title "The multiplier method" → "Using a multiplier"; common-mistake/quick-summary mentions of "the multiplier method" softened.
- `01-13-percentages/p3.md`: "shortcut" → "in one step".
- `01-13-percentages/p4.md`: "The multiplier method" reference removed; "The trap students fall into" reworded for clarity.

### 1.14 Using a calculator
- `01-14-using-a-calculator/p1.md`: "decider" stripped from check explain text and worked example.

### 1.15 Time
- `01-15-time/p2.md`: "break the calculation into easy chunks" → "break the gap into a few easy pieces"; mark annotations changed from "M1 for breaking into chunks" → "M1 for breaking into pieces"; common mistakes rewritten; quick summary updated.
- `01-15-time/p3.md`: "the chunk method" stripped throughout; "two-chunk method" → "split the journey at midnight and add the two pieces"; mark annotations updated.
- `01-15-time/review.md`: "the chunk method" stripped; mark-pattern table updated.

### 1.16 Money
- `01-16-money/p1.md`: "the multiplier method" softened; "Pad with trailing zeros" / "always pad with zeros" → "add zeros at the end so both numbers have the same length" / "always add the zeros"; "The trick is to convert time to decimal hours" → "The key step is to convert the time into decimal hours".
- `01-16-money/p2.md`: "The questions are simple if you know the trick" → "The questions are simple if you remember one rule".

### 1.17 Exponential growth and decay
- `01-17-exponential-growth-and-decay/p1.md`: "shortcut" softened in two places ("(in one step)" inside a diagram caption; "The shortcut: for any..." rewritten without the framing); "The trick" → "The key step"; "until the inequality flips" → "until the result first goes over the target".
- `01-17-exponential-growth-and-decay/p2.md`: "until the inequality flips" → "until the result first lands below the target" (twice).
- `01-17-exponential-growth-and-decay/review.md`: "until the inequality flips" → "until the result first crosses the target".

### 1.18 Surds
- `01-18-surds/p1.md`: "The trick" → "The idea"; "The shortcut" → "A few quick checks".
- `01-18-surds/p2.md`: "the conjugate trick that kills the surd" → "the conjugate pattern that removes the surd, leaving a whole number"; "this trick" → "this".
- `01-18-surds/p3.md`: "the conjugate trick from p2" → "the conjugate idea from p2"; diagram caption "CONJUGATE TRICK" → "CONJUGATE PATTERN"; quick summary "the conjugate trick" → "the conjugate".

## Most common phrasing patterns replaced

| Before | After |
|---|---|
| "the decider" / "decider rule" | "the digit just after the cut" / "look at the next digit" |
| "pad with (trailing) zeros" | "add zeros at the end so both numbers have the same length" / "fill the gap with zeros" / "add zeros to keep the size" |
| "the multiplier method" (when used as a brand) | "use a single multiplier" / removed entirely; the technique is described as a step |
| "the unitary method" | "find one unit, then multiply" (kept "sometimes called the unitary method" once, since it's standard syllabus vocabulary) |
| "the chunk method" / "the two-chunk method" | "break the gap into pieces at the round hours" / "split the journey at midnight and add the two pieces" |
| "the trick is..." / "the shortcut is..." / "the recipe is..." | "the rule is..." / "the method is..." / "three steps:..." or just dropped, leaving the actual instruction |
| "lens-shaped overlap" | "the bit in the middle where the two circles cross over" |
| "kills the sign" / "kills the surd" / "eats the irrationality" | "always gives a positive answer" / "removes the surd, leaving a whole number" |
| "the anchor" (for decimal point) | "stack the numbers so the decimal points sit directly above each other" |
| "the conjugate trick" | "the conjugate pattern" / "the conjugate" |
| "until the inequality flips" | "until the result first crosses the target" |
| "Cambridge marks the form" | kept where it appears once per part; trimmed where it had become repetitive |

## Files left unchanged

The following parts were already in clean teacher-voice and didn't need changes: `01-01/p1`, `01-01/p2`, `01-01/p3`, `01-01/p4`, `01-01/p5`, `01-02/p1`, `01-02/p2`, `01-02/review`, `01-03/p2`, `01-03/p3`, `01-03/review`, `01-04/p2`, `01-04/p3`, `01-04/review`, `01-05/p1`, `01-05/review`, `01-06/p4`, `01-06/review`, `01-07/p1`, `01-07/p3`, `01-07/review`, `01-08/p1`, `01-08/p3`, `01-08/review`, `01-10/p1`, `01-10/p3`, `01-10/review`, `01-12/p3`, `01-12/review`, `01-14/p2`, `01-14/review`, `01-15/p1`, `01-16/review`, `01-18/review`, plus the diagram-only blocks throughout.

## Judgement calls

- **"Keep, flip, multiply"** for fraction division — kept (it's standard O-Level teacher vocabulary, and a real teacher in a Lahore classroom would say exactly that). Removed only the framing "the recipe is".
- **"The unitary method"** — kept once with "sometimes called", because it's an actual term in the Cambridge syllabus and students will hear it from teachers. Did not promote it as the topic's primary label, though.
- **"Cover the letter you want"** for the speed/density triangle — kept; this is a classic teacher gesture, not a textbook-author gimmick.
- **"Hungry mouth eating the bigger number"** for $<$ vs $>$ — kept; it's a standard primary-school mnemonic that a Pakistani student will already have heard.
- **"Memory hooks: U is for tUgether..."** in 1.2 p2 — kept; these are real teacher mnemonics, not invented branding.
- **"Burn in"** the sums-and-quotients table in 1.10 p3 — left as-is; it's idiomatic teacher language ("memorise this") rather than a clever invention.
- **CSS class names** like `dtype-kicker` — left untouched; they're structural code, not user-facing prose.
