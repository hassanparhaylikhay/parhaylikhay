---
title: Expanding brackets
checks:
  - q: 'Expand $3(2x + 5)$.'
    options: ["$6x + 5$", "$6x + 15$", "$2x + 15$", "$5x + 15$"]
    correct: 1
    explain: 'Multiply each term inside the bracket by $3$: $3 \cdot 2x = 6x$ and $3 \cdot 5 = 15$. So $3(2x + 5) = 6x + 15$.'
  - q: 'Expand $-2(x - 4)$.'
    options: ["$-2x - 8$", "$-2x + 8$", "$2x - 8$", "$-2x - 4$"]
    correct: 1
    explain: 'Each term inside is multiplied by $-2$. $-2 \cdot x = -2x$. $-2 \cdot (-4) = +8$. Result: $-2x + 8$.'
  - q: 'Expand $(x + 3)(x + 2)$.'
    options: ["$x^2 + 5$", "$x^2 + 6$", "$x^2 + 5x + 6$", "$x^2 + 6x + 5$"]
    correct: 2
    explain: 'Each term in the first bracket multiplies each term in the second: $x \cdot x + x \cdot 2 + 3 \cdot x + 3 \cdot 2 = x^2 + 2x + 3x + 6 = x^2 + 5x + 6$.'
  - q: 'Expand $(2x - 1)(x + 4)$.'
    options: ["$2x^2 + 7x - 4$", "$2x^2 - 7x - 4$", "$2x^2 + 9x - 4$", "$2x^2 + 8x - 4$"]
    correct: 0
    explain: '$2x \cdot x + 2x \cdot 4 + (-1) \cdot x + (-1) \cdot 4 = 2x^2 + 8x - x - 4 = 2x^2 + 7x - 4$.'
  - q: 'Expand $(x + 1)(x + 2)(x + 3)$.'
    options: ["$x^3 + 6x^2 + 11x + 6$", "$x^3 + 6$", "$x^3 + 5x^2 + 6x + 6$", "$x^3 + 11x^2 + 6x$"]
    correct: 0
    explain: 'Expand two brackets first: $(x+1)(x+2) = x^2 + 3x + 2$. Then multiply by the third: $(x^2 + 3x + 2)(x + 3) = x^3 + 3x^2 + 3x^2 + 9x + 2x + 6 = x^3 + 6x^2 + 11x + 6$.'
---

## Why this matters

Imagine your group of $4$ friends each orders $1$ burger and $2$ fries. The total is $4 \times (1 \text{ burger} + 2 \text{ fries})$. You instinctively distribute the $4$: $4$ burgers $+$ $8$ fries. That distributing-of-multiplication-over-addition is exactly **expanding a bracket** in algebra. Cambridge expects you to do it cleanly across single, double, and triple brackets, and to handle every sign correctly.

Expanding shows up in almost every algebra question, often as a small step inside a longer problem (solving an equation, simplifying a fraction, finding the area of a rectangle). Get the patterns automatic and the rest of the topic eases up.

## Single bracket: distribute the multiplier

For an expression like $a(b + c)$, the multiplier $a$ multiplies **every term inside** the bracket:

$$a(b + c) \;=\; ab + ac$$

The same rule with three terms:

$$a(b + c + d) \;=\; ab + ac + ad$$

The geometric way to see this: a rectangle of width $a$ and height $(b + c)$ has area $a(b + c)$. Splitting the height into two strips of $b$ and $c$ gives areas $ab$ and $ac$. Same total, written two ways.

<div class="diagram">
<div class="diagram-caption">RECTANGLE AREA MODEL · \(a(b + c) = ab + ac\)</div>
<div style="display:flex;justify-content:center">
<svg viewBox="0 0 320 180" preserveAspectRatio="xMidYMid meet" style="display:block;width:100%;max-width:380px;height:auto" xmlns="http://www.w3.org/2000/svg">
<rect x="60" y="30" width="220" height="60" fill="rgba(255,240,103,0.08)" stroke="#fff067" stroke-width="1.5"/>
<rect x="60" y="90" width="220" height="50" fill="rgba(15,238,137,0.08)" stroke="#0fee89" stroke-width="1.5"/>
<text x="170" y="65" font-family="Geist Mono,monospace" font-size="22" fill="#fff067" font-weight="700" text-anchor="middle">ab</text>
<text x="170" y="120" font-family="Geist Mono,monospace" font-size="22" fill="#0fee89" font-weight="700" text-anchor="middle">ac</text>
<text x="40" y="64" font-family="Geist Mono,monospace" font-size="14" fill="#fff067" text-anchor="middle">b</text>
<text x="40" y="119" font-family="Geist Mono,monospace" font-size="14" fill="#0fee89" text-anchor="middle">c</text>
<text x="170" y="160" font-family="Geist Mono,monospace" font-size="14" fill="#7a7875" text-anchor="middle">a</text>
<line x1="60" y1="155" x2="280" y2="155" stroke="#7a7875" stroke-width="1"/>
<line x1="60" y1="151" x2="60" y2="159" stroke="#7a7875" stroke-width="1"/>
<line x1="280" y1="151" x2="280" y2="159" stroke="#7a7875" stroke-width="1"/>
</svg>
</div>
<div style="text-align:center;margin-top:6px;font-family:var(--font-geist-mono),monospace;font-size:13px;color:#7a7875">total area = ab + ac, same as multiplying \(a\) by \((b+c)\)</div>
</div>

The multiplier can be a number, a letter, or a combination (like $-3x$). It still distributes over every term inside.

**Examples.**

- $3(2x + 5) = 6x + 15$
- $5(x - 4) = 5x - 20$
- $x(x + 7) = x^2 + 7x$
- $2x(3x - 1) = 6x^2 - 2x$

## A negative multiplier flips signs

When the multiplier is negative, **every term inside flips sign**.

$$-(x - 4) \;=\; -x + 4$$

$$-2(x - 5) \;=\; -2x + 10$$

The minus is a $-1$ being distributed through, so each term gets multiplied by $-1$ in addition to whatever number is there. A minus times a minus gives a plus.

<div class="trap-callout">

**Trap: don't lose the second sign.** $-2(x - 4)$ becomes $-2x + 8$, NOT $-2x - 8$. The $-2$ multiplies the $-4$ to give $+8$. Whenever a negative multiplier meets a minus inside, write the result with brackets first ($(-2)(-4)$) and then evaluate.

</div>

## Double brackets: each term in one meets each term in the other

For $(a + b)(c + d)$, every term in the first bracket multiplies every term in the second. Four products in total:

$$(a + b)(c + d) \;=\; ac + ad + bc + bd$$

A useful mnemonic some teachers use is **FOIL** (First, Outer, Inner, Last), describing which pairs to multiply. Write all four and combine like terms at the end.

<div class="diagram">
<div class="diagram-caption">RECTANGLE AREA MODEL · \((a + b)(c + d)\)</div>
<div style="display:flex;justify-content:center">
<svg viewBox="0 0 320 220" preserveAspectRatio="xMidYMid meet" style="display:block;width:100%;max-width:380px;height:auto" xmlns="http://www.w3.org/2000/svg">
<rect x="70" y="30" width="120" height="80" fill="rgba(255,240,103,0.08)" stroke="#fff067" stroke-width="1.5"/>
<rect x="190" y="30" width="80" height="80" fill="rgba(15,238,137,0.08)" stroke="#0fee89" stroke-width="1.5"/>
<rect x="70" y="110" width="120" height="60" fill="rgba(0,171,250,0.08)" stroke="#00abfa" stroke-width="1.5"/>
<rect x="190" y="110" width="80" height="60" fill="rgba(255,130,44,0.08)" stroke="#ff822c" stroke-width="1.5"/>
<text x="130" y="76" font-family="Geist Mono,monospace" font-size="20" fill="#fff067" font-weight="700" text-anchor="middle">ac</text>
<text x="230" y="76" font-family="Geist Mono,monospace" font-size="20" fill="#0fee89" font-weight="700" text-anchor="middle">ad</text>
<text x="130" y="148" font-family="Geist Mono,monospace" font-size="20" fill="#00abfa" font-weight="700" text-anchor="middle">bc</text>
<text x="230" y="148" font-family="Geist Mono,monospace" font-size="20" fill="#ff822c" font-weight="700" text-anchor="middle">bd</text>
<text x="130" y="20" font-family="Geist Mono,monospace" font-size="14" fill="#7a7875" text-anchor="middle">c</text>
<text x="230" y="20" font-family="Geist Mono,monospace" font-size="14" fill="#7a7875" text-anchor="middle">d</text>
<text x="50" y="74" font-family="Geist Mono,monospace" font-size="14" fill="#7a7875" text-anchor="middle">a</text>
<text x="50" y="146" font-family="Geist Mono,monospace" font-size="14" fill="#7a7875" text-anchor="middle">b</text>
<text x="170" y="200" font-family="Geist Mono,monospace" font-size="13" fill="#7a7875" text-anchor="middle">total area = ac + ad + bc + bd</text>
</svg>
</div>
</div>

After computing all four pieces, **combine any like terms** to write the expansion in simplest form.

**Example.** Expand $(x + 3)(x + 2)$.

$$x \cdot x + x \cdot 2 + 3 \cdot x + 3 \cdot 2 \;=\; x^2 + 2x + 3x + 6 \;=\; x^2 + 5x + 6$$

The two middle terms ($2x$ and $3x$) are like terms; they combine to $5x$.

## Try the area model live

Drag the $a$, $b$, $c$, $d$ sliders below. The four sub-rectangles update in real time, and the four products $ac, ad, bc, bd$ underneath show what gets summed for the expansion.

<iframe src="/widgets/area-model-expander.html" style="width:100%;max-width:560px;height:420px;border:0;border-radius:10px;display:block;margin:24px auto" loading="lazy"></iframe>

## Triple brackets: expand two, then multiply by the third

For $(p)(q)(r)$, expand any two of the three brackets first to get an expression, then multiply that result by the remaining bracket. The order doesn't change the answer.

**Example.** Expand $(x + 1)(x + 2)(x + 3)$.

**Step 1: Expand the first two.**

$$(x + 1)(x + 2) \;=\; x^2 + 3x + 2$$

**Step 2: Multiply that by the third bracket.**

$$(x^2 + 3x + 2)(x + 3)$$

Each term in the first bracket meets each term in the second. Six products this time:

$$x^2 \cdot x + x^2 \cdot 3 + 3x \cdot x + 3x \cdot 3 + 2 \cdot x + 2 \cdot 3$$

$$= \;x^3 + 3x^2 + 3x^2 + 9x + 2x + 6$$

**Step 3: Combine like terms.**

$$x^3 + 6x^2 + 11x + 6$$

Triple-bracket questions are worth 2 to 3 marks: M for the first expansion, M for the second, A for the simplified answer.

## Worked example

Expand and simplify $(2x - 1)(x + 4)$.

**Step 1: Multiply each term in the first bracket by each term in the second.**

$$2x \cdot x + 2x \cdot 4 + (-1) \cdot x + (-1) \cdot 4$$

$$= \;2x^2 + 8x - x - 4$$

**Step 2: Combine like terms.**

$$2x^2 + 7x - 4$$

**Answer:** $\boxed{2x^2 + 7x - 4}$. **[M1 for full expansion; A1 for the simplified form]**

Full marks $= 2$.

## Common mistakes

- Forgetting one of the four products in a double bracket. $(a+b)(c+d)$ has FOUR products, not three.
- Sign errors with negative multipliers. $-(x - 5) = -x + 5$, NOT $-x - 5$.
- Treating $(x + 3)^2$ as $x^2 + 9$. It's actually $(x+3)(x+3) = x^2 + 6x + 9$. The middle terms matter.
- Adding instead of multiplying powers. $x \cdot x = x^2$, not $2x$.

## Quick summary

- Single bracket: the multiplier hits **every** term inside.
- Double bracket: each term in the first meets each in the second (FOIL gives four products).
- Negative multiplier: every term inside flips sign.
- Triple bracket: expand any two first, then multiply by the third.
- Always **combine like terms** at the end.

## Your turn: drill expanding

The widget below mixes single, double and triple brackets across positive and negative coefficients.
