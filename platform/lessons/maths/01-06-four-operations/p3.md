---
title: 'Decimals: + − × ÷'
checks:
  - q: 'Compute $1.5 + 0.27$.'
    options: ["1.32", "1.42", "1.77", "1.87"]
    correct: 2
    explain: 'Line up the decimal points: $1.50 + 0.27 = 1.77$. Writing $1.5$ as $1.50$ helps you see the columns.'
  - q: 'Compute $4.2 - 1.85$.'
    options: ["2.35", "2.45", "3.05", "3.45"]
    correct: 0
    explain: 'Write as $4.20 - 1.85$ so both numbers have the same length. Borrow from the tenths: $4.20 - 1.85 = 2.35$.'
  - q: 'Compute $0.4 \times 0.3$.'
    options: ["0.12", "1.2", "0.012", "0.7"]
    correct: 0
    explain: 'Multiply as integers: $4 \times 3 = 12$. Total decimal places: $1 + 1 = 2$. So $0.12$.'
  - q: 'Compute $1.6 \div 0.4$.'
    options: ["0.4", "0.04", "4", "40"]
    correct: 2
    explain: 'Shift both numbers by one place: $\tfrac{1.6}{0.4} = \tfrac{16}{4} = 4$. Move the decimal point in both until the divisor is a whole number.'
---

## Why this matters

Decimal arithmetic on Paper 1 is non-calculator, so Cambridge expects you to handle it cleanly by hand. The key is that addition/subtraction need decimal points lined up, while multiplication needs you to count decimal places. Two different mental moves.

## Adding and subtracting: line up the decimal points

Stack the numbers so the decimal points sit directly above each other. That keeps the same place values in the same column. If one number has fewer decimal places, add zeros at the end so both numbers have the same length.

<div class="diagram">
<div class="diagram-caption">\(1.5 + 0.27\) · ALIGN THE DOTS</div>
<div style="display:flex;flex-direction:column;align-items:center;font-family:var(--font-geist-mono),monospace;font-size:20px;color:#f0eeea;line-height:1.2">
<div style="white-space:pre">&nbsp;&nbsp;1<span style="color:#fff067">.</span>5<span style="color:#7a7875">0</span></div>
<div style="white-space:pre">+ 0<span style="color:#fff067">.</span>27</div>
<div style="white-space:pre;color:#7a7875">─────</div>
<div style="white-space:pre">&nbsp;&nbsp;<span style="color:#0fee89;font-weight:700">1</span><span style="color:#fff067">.</span><span style="color:#0fee89;font-weight:700">77</span></div>
</div>
</div>

The extra zero after $1.5$ (making it $1.50$) just makes the columns line up clearly. It doesn't change the value.

For subtraction, same alignment. Borrow from the column on the left when needed: $4.20 - 1.85 = 2.35$.

## Multiplying: count the decimal places

Multiplication doesn't care about lining up the points. Instead:

1. **Multiply as if both numbers were whole numbers** (ignore the decimal points).
2. **Count the total number of decimal places** in the original two numbers.
3. **Place the decimal point** in the answer that many places from the right.

<div class="diagram">
<div class="diagram-caption">\(0.4 \times 0.3\) · COUNT THE PLACES</div>
<div style="display:grid;grid-template-columns:auto 1fr;gap:10px 18px;align-items:center;font-size:14px">
<div style="display:inline-flex;align-items:center;justify-content:center;width:28px;height:28px;border-radius:50%;background:rgba(0,171,250,0.14);border:1px solid rgba(0,171,250,0.5);color:#00abfa;font-family:var(--font-geist-mono),monospace;font-size:13px;font-weight:700">1</div>
<div>Multiply as integers: \(4 \times 3 = 12\).</div>
<div style="display:inline-flex;align-items:center;justify-content:center;width:28px;height:28px;border-radius:50%;background:rgba(255,240,103,0.14);border:1px solid rgba(255,240,103,0.5);color:#fff067;font-family:var(--font-geist-mono),monospace;font-size:13px;font-weight:700">2</div>
<div>Total decimal places in inputs: \(\textcolor{#fff067}{1} + \textcolor{#fff067}{1} = 2\) (one each from \(0.4\) and \(0.3\)).</div>
<div style="display:inline-flex;align-items:center;justify-content:center;width:28px;height:28px;border-radius:50%;background:rgba(15,238,137,0.14);border:1px solid rgba(15,238,137,0.5);color:#0fee89;font-family:var(--font-geist-mono),monospace;font-size:13px;font-weight:700">3</div>
<div>Place the decimal \(\textcolor{#0fee89}{2}\) from the right of \(12\), giving \(\textcolor{#0fee89}{0.12}\).</div>
</div>
</div>

Quick sanity check: $0.4 \approx 0.5$ and $0.3 \approx 0.3$, so the answer should be roughly $0.5 \times 0.3 = 0.15$. We got $0.12$, close. Good.

A second example: $1.2 \times 0.05$. Integers: $12 \times 5 = 60$. Total places: $1 + 2 = 3$. Place the decimal $3$ from the right of $60$ (which is $060$): $0.060 = 0.06$.

## Dividing: make the divisor a whole number

Dividing by a decimal is awkward. To make it easier, **shift the decimal point in BOTH numbers** until the divisor is a whole number. As long as you shift both by the same amount, the answer stays the same.

<div class="diagram">
<div class="diagram-caption">\(1.6 \div 0.4\) · SHIFT BOTH</div>
<div style="font-family:var(--font-geist-mono),monospace;font-size:16px;color:#f0eeea;line-height:1.7;text-align:center">
\(\dfrac{1.6}{0.4} \;\xrightarrow{\text{shift both by 1 place}}\; \dfrac{16}{4} \;=\; 4\)
</div>
</div>

You shifted $0.4 \to 4$ (one place right) and $1.6 \to 16$ (one place right): same shift, value unchanged. Now it's plain integer division.

For $0.84 \div 0.4$: shift by 1 to get $\tfrac{8.4}{4} = 2.1$. (You can shift by less if it's already enough to clear the divisor.)

For $0.84 \div 0.04$: shift by 2 to get $\tfrac{84}{4} = 21$.

<div class="trap-callout">

**Trap: the answer can be larger than what you started with.** $1.6 \div 0.4 = 4$, which is bigger than $1.6$. That's correct: dividing by something less than 1 makes the answer bigger. Don't be surprised when the result jumps up.

</div>

## Worked example

A pen costs \$0.85. How much do 12 pens cost?

**Step 1: Multiply as integers.** $85 \times 12 = 1020$.

(Mental: $85 \times 12 = 85 \times 10 + 85 \times 2 = 850 + 170 = 1020$.)

**Step 2: Count decimal places.** $0.85$ has 2 places, $12$ has 0 places, total = $2$.

**Step 3: Place the decimal.** $1020$ with the decimal $2$ from the right is $10.20$.

**Answer:** \$$\boxed{10.20}$. **[M1 for the integer multiplication; A1 for the correct decimal placement]**

Full marks $= 2$.

## Common mistakes

- Lining numbers up by their right edges instead of by the decimal point. $1.5 + 0.27$ is NOT $1.5\overline{2.7}$.
- Forgetting to count zeros in decimal places. $0.05$ has 2 decimal places, not 1.
- For division, shifting only one of the two numbers. Both have to shift the same amount.
- Treating $0.4 \times 0.3 = 1.2$. Don't lose the decimal places; count them.

## Quick summary

- <b style="color:#00abfa">+ / −</b>: line up the decimal points; add zeros so both numbers have the same length; then add or subtract column by column.
- <b style="color:#0fee89">×</b>: multiply as integers, then count total decimal places and place the point.
- <b style="color:#fff067">÷</b>: shift both numbers right until the divisor is a whole number, then divide normally.
