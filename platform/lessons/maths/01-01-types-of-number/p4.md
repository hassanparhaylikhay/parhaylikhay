---
title: Prime factorisation
checks:
  - q: "What is the prime factorisation of 60?"
    options: ["2 × 30", "6 × 10", "2² × 3 × 5", "4 × 15"]
    correct: 2
    explain: "60 = 2 × 2 × 3 × 5 = 2² × 3 × 5. We keep going until every factor is prime. The other answers still contain composite numbers (30, 6, 10, 4, 15)."
  - q: "In the factor tree for 72, the bottom row of primes is 2, 2, 2, 3, 3. Written in index form, 72 = ?"
    options: ["2³ × 3²", "2² × 3³", "6 × 12", "2⁵ × 3"]
    correct: 0
    explain: "There are three 2s and two 3s → 2³ × 3². Count carefully and always check: 2×2×2 = 8, and 3×3 = 9, and 8×9 = 72. ✓"
  - q: "Which of these numbers is already fully prime-factorised?"
    options: ["2 × 2 × 9", "3 × 12", "2² × 5", "6 × 2"]
    correct: 2
    explain: "Every factor must be prime. In the others, 9, 12, and 6 are all composite. Only 2² × 5 has primes throughout."
---

## What this part is about

**Every** whole number bigger than 1 can be broken down into a **unique** list of primes multiplied together. That list is called its **prime factorisation**, and it's the single most useful tool in the Number topic. Once you can factorise, HCF, LCM, finding square roots of big numbers, and recurring-decimal questions all become mechanical.

## The idea

A composite number is made by multiplying smaller numbers. Keep breaking those smaller numbers down, and you'll eventually hit numbers that can't be broken any further — the **primes**.

For example, $12$ can be written as:

$$12 = 2 \times 6 = 2 \times 2 \times 3$$

The $2$s and the $3$ can't be split any further. So the prime factorisation of $12$ is $2 \times 2 \times 3$.

Two important facts:

1. **Every number has exactly one prime factorisation.** No matter how you start breaking it down, you end up at the same list of primes (just possibly in a different order).
2. **Index notation keeps it tidy.** $2 \times 2 \times 3$ becomes $2^2 \times 3$. If $2$ appears five times, write $2^5$. Always write the primes in ascending order: $2^3 \times 5^2 \times 7$, not $7 \times 2^3 \times 5^2$.

## The factor tree method

A **factor tree** is the standard way to find a prime factorisation. Write the number at the top. Split it into any two factors. Split each factor again. Keep going until every branch ends at a prime.

Here's the tree for $360$:

<div class="diagram factor-tree">
  <div class="diagram-caption">FACTOR TREE FOR 360</div>

  <div class="ft-row"><span class="ft-node composite">360</span></div>
  <div class="ft-branches">╱ &nbsp; ╲</div>
  <div class="ft-row">
    <span class="ft-node prime">2</span>
    <span class="ft-node composite">180</span>
  </div>
  <div class="ft-branches" style="justify-content:flex-end; padding-right:14px">╱ &nbsp; ╲</div>
  <div class="ft-row">
    <span class="ft-node prime" style="visibility:hidden">2</span>
    <span class="ft-node prime">2</span>
    <span class="ft-node composite">90</span>
  </div>
  <div class="ft-branches" style="justify-content:flex-end; padding-right:14px">╱ &nbsp; ╲</div>
  <div class="ft-row">
    <span class="ft-node prime" style="visibility:hidden">2</span>
    <span class="ft-node prime" style="visibility:hidden">2</span>
    <span class="ft-node prime">2</span>
    <span class="ft-node composite">45</span>
  </div>
  <div class="ft-branches" style="justify-content:flex-end; padding-right:14px">╱ &nbsp; ╲</div>
  <div class="ft-row">
    <span class="ft-node prime" style="visibility:hidden">2</span>
    <span class="ft-node prime" style="visibility:hidden">2</span>
    <span class="ft-node prime" style="visibility:hidden">2</span>
    <span class="ft-node prime">3</span>
    <span class="ft-node composite">15</span>
  </div>
  <div class="ft-branches" style="justify-content:flex-end; padding-right:14px">╱ &nbsp; ╲</div>
  <div class="ft-row">
    <span class="ft-node prime" style="visibility:hidden">2</span>
    <span class="ft-node prime" style="visibility:hidden">2</span>
    <span class="ft-node prime" style="visibility:hidden">2</span>
    <span class="ft-node prime" style="visibility:hidden">3</span>
    <span class="ft-node prime">3</span>
    <span class="ft-node prime">5</span>
  </div>

  <p class="ft-result"><b>360</b> = 2 × 2 × 2 × 3 × 3 × 5 = <b>2³ × 3² × 5</b></p>
</div>

Read off the primes at the bottom, group them with indices, and you have your answer.

## Tips for building a tree quickly

1. **Start with the smallest prime that divides.** If the number is even, divide by 2. If not, try 3, then 5, then 7.
2. **Keep dividing by 2** until you can't any more, then move to 3, then 5, then 7, ….
3. **Stop when every leaf is prime.** Use the prime list from Part 3 to check.
4. **Write the final answer in index form,** primes in ascending order.

Any starting split works — you end up at the same primes — but starting with 2 usually keeps the numbers small.

## Worked example 1

Write $84$ as a product of its prime factors, using index notation.

**Working.**

$84$ is even, so divide by $2$:

$$84 = 2 \times 42$$

$42$ is also even:

$$84 = 2 \times 2 \times 21$$

$21$ is not even. Try $3$: $21 \div 3 = 7$.

$$84 = 2 \times 2 \times 3 \times 7$$

$7$ is prime — stop. **[M1 for a correct factor-tree method shown]**

In index form: $\boxed{84 = 2^2 \times 3 \times 7}$. **[A1]**

Full marks $= 2$.

## Worked example 2

$P = x^n y^2$ and $Q = x^{n-1} y^4$, where $x$ and $y$ are prime. Write $P \times Q$ as a product of prime factors, in index form.

*(This tests the law of indices $a^m \times a^n = a^{m+n}$ combined with prime factorisation notation — s23_12 Q14 style.)*

**Working.**

$$P \times Q = x^n y^2 \times x^{n-1} y^4$$

Multiply powers of the same prime by adding indices:

- $x$: $x^n \times x^{n-1} = x^{n + (n-1)} = x^{2n - 1}$ **[B1]**
- $y$: $y^2 \times y^4 = y^{2+4} = y^6$ **[B1]**

$$P \times Q = \boxed{x^{2n-1}\ y^6}$$

Full marks $= 2$.

> **Examiner tip:** Cambridge rewards showing the factor tree (or equivalent working) — **M1 for the method**, separate from the final answer. A student who writes "$84 = 2^2 \times 3 \times 7$" with no tree might still earn the answer mark but loses the method mark if present. Always show the breakdown.
