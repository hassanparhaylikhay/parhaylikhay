---
title: 'Range and which average to use'
checks:
  - q: 'The RANGE of a data set is...'
    options: ["largest value minus smallest value", "the middle value", "the most common value", "the sum divided by the count"]
    correct: 0
    explain: 'Range = max $-$ min. It measures the SPREAD, not the centre.'
  - q: 'For salary data with one billionaire and many average workers, the BEST average to report is the...'
    options: ["median", "mean", "mode", "range"]
    correct: 0
    explain: 'The billionaire is an OUTLIER. The mean would be huge and not representative. The median (middle salary) is unaffected by extremes.'
  - q: 'For "favourite ice-cream flavour" data, the only sensible average is the...'
    options: ["mode", "mean", "median", "range"]
    correct: 0
    explain: 'You cannot compute a mean or median of flavour names. The mode (most common flavour) is the only one that works for categorical data.'
  - q: 'A small range tells you the data is...'
    options: ["clustered tightly", "spread far apart", "negative", "exactly one value"]
    correct: 0
    explain: 'Small range = max and min are close = all values cluster together. Large range = values spread widely.'
  - q: 'If two data sets have the same mean but very different ranges, you should...'
    options: ["mention BOTH centre and spread when comparing them", "say they are identical", "ignore the spread", "use only the mode"]
    correct: 0
    explain: 'A complete comparison needs centre AND spread. Same mean but different ranges = same average performance, different consistency.'
---

## Why this matters

Two questions every statistics paper asks:

1. "What is the **range** of the data?", a measure of how spread out the values are.
2. "Which average is best to use here?", a justification question, picking mean vs median vs mode based on the situation.

Both are worth $1$ to $2$ marks. The skill is short and memorisable.

## Range

The **range** measures SPREAD. It is the largest value minus the smallest:

$$\text{range} = \text{max} - \text{min}$$

The data set $3, 5, 5, 6, 7, 8$ has range $8 - 3 = 5$.

A small range means the data values are close together. A large range means they are spread out. The range is the simplest measure of spread on the O-Level paper (the interquartile range, IQR, comes in 9.6).

A trap: the range tells you nothing about what is in between. Two data sets with the same range can still look very different in shape. So the range is paired with an average for a fuller picture.

## When to use the mean

The MEAN is the default average for numeric data without extreme values. It uses every data point.

Use the mean when:

- The data has no extreme outliers.
- You need an answer that respects every value.
- The data is going to be combined or compared with other means.

Avoid the mean when:

- One or two extreme values would distort the picture.
- The data is categorical (no numerical values).

## When to use the median

The MEDIAN is the average to choose when there are OUTLIERS. The median is robust, adding one extreme value barely moves it.

Use the median when:

- The data has outliers (a few very large or very small values).
- You want a "typical" value resistant to extremes.
- Examples: house prices, salaries, marathon finish times in a race with one or two superstars.

## When to use the mode

The MODE is the only average that works for non-numeric data ("favourite fruit", "primary language", "shoe size category").

Use the mode when:

- The data is categorical.
- You want the most popular or most common option.
- Examples: most popular bus route, most common shoe size sold, most common eye colour.

## A side-by-side comparison

<div class="diagram" style="border:1px solid #141e2a;border-radius:10px;padding:14px 16px;margin:14px 0;background:#0b1118">
  <div style="font-family:'Geist Mono',monospace;font-size:11px;color:#7a7875;letter-spacing:0.4px;margin-bottom:14px;text-transform:uppercase;text-align:center">PICK YOUR AVERAGE</div>
  <div style="display:flex;gap:10px;align-items:stretch;flex-wrap:wrap">
    <div style="flex:1;min-width:140px;padding:12px;border:1.5px solid rgba(0,171,250,0.4);border-radius:8px;background:rgba(0,171,250,0.04)">
      <div style="color:#00abfa;font-family:'Geist Mono',monospace;font-size:10.5px;letter-spacing:0.5px;margin-bottom:6px">MEAN</div>
      <div style="font-size:13px;color:#f0eeea;line-height:1.5">numeric, no outliers, want every value to count</div>
    </div>
    <div style="flex:1;min-width:140px;padding:12px;border:1.5px solid rgba(255,70,112,0.4);border-radius:8px;background:rgba(255,70,112,0.04)">
      <div style="color:#ff4670;font-family:'Geist Mono',monospace;font-size:10.5px;letter-spacing:0.5px;margin-bottom:6px">MEDIAN</div>
      <div style="font-size:13px;color:#f0eeea;line-height:1.5">numeric with outliers; need a robust typical value</div>
    </div>
    <div style="flex:1;min-width:140px;padding:12px;border:1.5px solid rgba(15,238,137,0.4);border-radius:8px;background:rgba(15,238,137,0.04)">
      <div style="color:#0fee89;font-family:'Geist Mono',monospace;font-size:10.5px;letter-spacing:0.5px;margin-bottom:6px">MODE</div>
      <div style="font-size:13px;color:#f0eeea;line-height:1.5">categorical data, or want "most popular"</div>
    </div>
  </div>
  <div style="font-family:'Geist Mono',monospace;font-size:11.5px;color:#7a7875;text-align:center;margin-top:12px;line-height:1.5">pick the one whose strength matches the question</div>
</div>

## Worked example: choose the average

A company has $20$ employees. $19$ earn around $\$30{,}000$ per year. The CEO earns $\$2{,}000{,}000$. Which average best describes a "typical" salary?

**Mean** would be $(19 \times 30{,}000 + 2{,}000{,}000) / 20 = 2{,}570{,}000 / 20 = \$128{,}500$.

That is not a typical salary at this company. Nobody actually earns near $\$128{,}500$.

**Median**: sort the salaries. The middle two are both $\$30{,}000$. So median $= \$30{,}000$.

That is the typical salary. The mean was distorted by the CEO.

**Answer:** the MEDIAN ($\$30{,}000$) is the more informative average for this data. **[B1 for the calculation; B1 for naming median with a one-line justification]**

Full marks $= 2$.

The marker wants the choice AND a reason.

## Common mistakes

- **Confusing range with the data values themselves.** Range is ONE number (max $-$ min).
- **Using the mean for skewed data** (data with outliers).
- **Computing the mean of categorical data**. You cannot average "apple, banana, cherry".
- **Picking the highest average** without thinking about which is appropriate. The question asks for the most INFORMATIVE average, not the largest one.

## Quick summary

- **Range** $= \text{max} - \text{min}$. Measures spread.
- **Mean**: use for numeric data without outliers.
- **Median**: use when there are outliers, for a robust typical value.
- **Mode**: use for categorical data or "most popular".
- Always JUSTIFY your choice in one sentence on the exam.
