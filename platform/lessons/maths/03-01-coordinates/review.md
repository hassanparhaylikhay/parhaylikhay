---
title: Review & practice
checks:
  - q: 'The point $(0, -4)$ is on...'
    options: ["the x-axis", "the y-axis", "Q3", "Q4"]
    correct: 1
    explain: 'x-coordinate is $0$, so the point is on the y-axis (specifically, $4$ units below the origin).'
  - q: 'The point $(2, 0)$ is...'
    options: ["the origin", "on the x-axis", "in Q1", "on the y-axis"]
    correct: 1
    explain: 'y-coordinate is $0$, so it sits on the x-axis ($2$ units to the right of the origin).'
  - q: 'A point in Q4 has...'
    options: ["$x > 0, y > 0$", "$x < 0, y < 0$", "$x > 0, y < 0$", "$x < 0, y > 0$"]
    correct: 2
    explain: 'Q4 (bottom-right): positive x, negative y.'
  - q: 'Reflecting $(5, 3)$ in the y-axis gives...'
    options: ["$(5, -3)$", "$(-5, 3)$", "$(-5, -3)$", "$(3, 5)$"]
    correct: 1
    explain: 'Reflecting in the y-axis flips the sign of x. So $(5, 3) \rightarrow (-5, 3)$.'
  - q: 'Reflecting $(2, -4)$ in the x-axis gives...'
    options: ["$(2, 4)$", "$(-2, -4)$", "$(-2, 4)$", "$(4, -2)$"]
    correct: 0
    explain: 'Reflecting in the x-axis flips the sign of y. So $(2, -4) \rightarrow (2, 4)$.'
---

## What you should know by now

If you've worked through P1 you can:

- Read and write coordinates in the form $(x, y)$ — x first, y second.
- Identify the origin, the x-axis, and the y-axis.
- Name the quadrant a point sits in (or recognise points that sit on an axis).
- Plot a point from its coordinates by going across first (x), then up or down (y).

## The four quick rules

1. The x-coordinate is the distance from the **y-axis**, not from the origin.
2. The y-coordinate is the distance from the **x-axis**, not from the origin.
3. Sign matters: $-3$ means LEFT (or DOWN), not just "three".
4. Points on an axis don't belong to a quadrant.

## Worked example

Plot the points $A(3, 2)$, $B(-3, 2)$, $C(-3, -2)$, $D(3, -2)$ and state what shape they form.

**Step 1.** Plot $A$ at $(3, 2)$ — Q1.

**Step 2.** Plot $B$ at $(-3, 2)$ — Q2.

**Step 3.** Plot $C$ at $(-3, -2)$ — Q3.

**Step 4.** Plot $D$ at $(3, -2)$ — Q4.

**Step 5.** Connect $A \to B \to C \to D \to A$.

**Step 6.** Identify: $AB$ and $CD$ have length $6$ (horizontal); $BC$ and $DA$ have length $4$ (vertical). All angles are right angles. So the shape is a rectangle.

**Answer:** $ABCD$ is a rectangle with sides $6$ and $4$. **[B1 plot all 4 points; B1 identify shape]**

Full marks $= 2$.

## Quick reference

- $(x, y)$ — x first, y second.
- Origin: $(0, 0)$.
- Q1: $x > 0, y > 0$. Q2: $x < 0, y > 0$. Q3: $x < 0, y < 0$. Q4: $x > 0, y < 0$.
- y-axis: $x = 0$. x-axis: $y = 0$.

## Your turn: full quiz

The widget below is a mixed quiz covering coordinates and quadrants.
